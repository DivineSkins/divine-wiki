name: crowdin-download

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

# Pushes the crowdin-translations branch and opens the sync PR. Needs
# "Allow GitHub Actions to create and approve pull requests" enabled in
# Settings -> Actions -> General (set 2026-06-11).
permissions:
  contents: write
  pull-requests: write

jobs:
  download:
    name: Download translations from Crowdin
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Crowdin download
        uses: crowdin/github-action@52aa776766211d83d975df51f3b9c53c2f8ba35f # v2.16.3
        with:
          upload_sources: false
          upload_translations: false
          download_translations: true
          localization_branch_name: crowdin-translations
          create_pull_request: true
          pull_request_title: "i18n: sync translations from Crowdin"
          pull_request_body: "Automated translation sync. Review language-lead sign-off per locale before merge."
          pull_request_labels: "i18n,automated"
          pull_request_base_branch_name: main
          config: crowdin.yml
        env:
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
